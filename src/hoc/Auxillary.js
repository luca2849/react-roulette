/**
 * Functional component used as a container without adding a div wrapper
 * 
 * @param {object} props - Props passed to the Aux component
 */
const Auxillary = (props) => props.children;

export default Auxillary;